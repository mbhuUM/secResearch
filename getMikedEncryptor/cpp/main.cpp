#include <bits/stdc++.h>
#include <fstream>
#include <deque>
#include <stack>
#include <windows.h>
#include <iostream>
#include <fstream>


class crypto {
    private: 
        int key;
        std::string file;
        char c;
    public:
        
};

void readFile(std::string path) {
    std::ofstream file;
    file.open(path);
    
}
int main(int argv, char* argc[])
{
    
}
